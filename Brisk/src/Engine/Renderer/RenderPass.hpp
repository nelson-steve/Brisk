#pragma once

#include <memory>
#include <string>

namespace Brisk 
{
    //struct RenderTarget {
    //    std::string p_Name;
    //};

    //struct RenderPass {
    //    std::string p_Name;
    //    std::vector<RenderTarget> p_Inputs;
    //    std::vector<RenderTarget> p_Outputs;
    //};
};
